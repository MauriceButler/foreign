const parallel = require('./parallel');
const series = require('./series');
const seriesAll = require('./seriesAll');
const seriesPromise = require('./seriesPromise');

module.exports = {
    parallel,
    series,
    seriesAll,
    seriesPromise,
};
